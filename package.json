{
  "name": "crypto-predictor-pro",
  "version": "2.0.0",
  "description": "Crypto Predictor PRO with XGBoost, SQLite, WebSocket",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "train-btc": "node -e \"require('./learning_engine').trainXGBoost('BTC').then(()=>process.exit(0))\"",
    "train-eth": "node -e \"require('./learning_engine').trainXGBoost('ETH').then(()=>process.exit(0))\""
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.6",
    "express": "^4.22.2",
    "node-cron": "^4.2.1",
    "node-fetch": "^2.7.0",
    "sqlite3": "^6.0.1",
    "ws": "^8.16.0"
  }
}
